<h1><%= @doctor.name%></h1>

<ul>
  <% @doctor.appointments.each do |appt| %>
  <li> <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M")%> - <%= link_to appt.patient.name, patient_path(appt.patient) %></li>
  <% end %>
</ul>

<h5><%= appointment_datetime.strftime("%B %d, %Y  %H,%M") %></h5>
